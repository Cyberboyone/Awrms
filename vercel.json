{
  "buildCommand": "sh \"$(git rev-parse --show-toplevel)/scripts/build-vercel.sh\"",
  "outputDirectory": "public",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": "other",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
